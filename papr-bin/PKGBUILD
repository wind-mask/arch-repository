# Maintainer: wind_mask <YOU KONW WHO>
pkgname=papr-bin
_pkgname=papr
provides=('papr')
conflicts=('papr')
pkgver=0.16.1
pkgrel=1
pkgdesc="A fast, native RSS reader — plus an agent-facing CLI so your AI agent can read, search and triage your feeds from the shell."
arch=('x86_64')
url="https://github.com/l0ng-ai/${_pkgname}"
license=('MIT')
depends=('cairo'  'gdk-pixbuf2' 'glib2' 'gtk3' 'hicolor-icon-theme' 'webkit2gtk-4.1' 'libsoup3' 'libxcb' 'glibc' 'gcc-libs')
options=('!strip' '!debug')
source_x86_64=("${url}/releases/download/v${pkgver//_/-}/${_pkgname}_${pkgver}_amd64.deb")
sha256sums_x86_64=('d99086f502377619e164495efeb3eef9a36518f705c2dc6aa7f8d33dd2599074')
package() {
  # Extract package data
  tar -xvf data.tar.gz -C "${pkgdir}"

}
